# --- Security / core ---
# Generate once locally and paste the result (do not keep the command here):
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=PASTE_YOUR_RANDOM_SECRET_KEY_HERE
# Use 'true' for local dev; 'false' in production
DEBUG=true
ALLOWED_HOSTS=127.0.0.1,localhost

# Frontend & CSRF/CORS
FRONTEND_URL=http://localhost:3000

# Google OAuth
GOOGLE_CLIENT_ID=Your_Google_Client_ID_Here
GOOGLE_CLIENT_SECRET=You_Google_Client_Secret_Here

# --- Database ---
# Option A: single URL (used outside docker-compose or on PaaS)
# Leave empty to fall back to SQLite in local bare-metal runs.
DATABASE_URL=postgresql://user:pass@host:5432/dbname

# --- ML artifacts (model loader paths) ---
MODEL_DIR=artifacts
MODEL_FILE=model.keras
PREPROC_FILE=preproc.pkl
META_FILE=meta.json
MODEL_DEFAULT_FAMILY=cnn_lstm_attn